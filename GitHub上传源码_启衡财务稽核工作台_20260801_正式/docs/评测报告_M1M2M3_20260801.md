# M1-M3 评测报告（2026-08-01）

## 已完成的本地回归

执行命令：

```powershell
python -m unittest discover -s tests -v
```

结果：**37/37 通过**。覆盖包括：

- 报销金额、附件、重复票、审批/特批、费用标准和写回 payload；
- 人工确认、改判、校准后二次确认、退回待审与审计留痕；
- 写回前预检：同结论不误标冲突，ERP 已有意见不覆盖；
- OCR 不可读、原票/台账冲突、电子客票台账缺失或不一致、人工确认错误字段后的整单重判；
- M3 风险矩阵完整性与 108 个内部异常场景覆盖。

## EXE 构建自检

- PyInstaller 构建成功：`dist/启衡财务稽核工作台_M1M2M3_交付版_20260801.exe`。
- 无界面启动检查通过。
- 归档检查未发现 API Key、人工操作缓存、`m4_matcher` 或 M4 交付依赖。
- Qt 冒烟检查：工作台可实例化，导航为 `m2, public, write, m3, cal, quality, settings`，无 M4 菜单。

## 隔离 ERP 最终验收（2026-08-01）

本节仅记录本次隔离 Docker 环境（API `127.0.0.1:18081`）的真实结果；批处理仅读取单据、审批、附件、发票台账并在本地 OCR，没有批量回写。

| 项目 | 本次结果 |
| --- | --- |
| 公开 30 单回归 | 判定 **30/30** 一致；违规代码 **30/30** 一致 |
| M2 全量批次 | `20260801-031124`；300/300 完成、0 失败；耗时 17 分 30 秒 |
| M2 分布 | 建议通过 118；建议驳回 147；转人工复核 35 |
| 车票规则回归 | `BX-005693`：关联台账购买方/税号不符时转二次人工复审；人工确认错误字段后整单重判为驳回 |
| M3 全量扫描 | 23,461 条台账；40 组重复票；110 条正式问题（税率 27、税号 39、抬头 44） |
| 写回安全闭环 | 对隔离单据 `BX202606-5697` 显式写入 1 条 AI 审核意见后回读成功；第二次请求跳过已有相同意见，未覆盖 |

OCR 字段人工抽样评测另见 `evals/ocr-field-report.json`：30 个样本、26 张可人工确认票据、4 张原票不可读；该指标仅用于字段提取质量，不宣称为审核结论准确率。

说明：正式 `submission.json` 仍需填入可访问的 GitHub 仓库链接后才能作为课程最终上传件；不会以虚构链接替代。
