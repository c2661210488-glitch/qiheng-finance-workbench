# 交付说明（2026-08-01）

本交付包范围为 M1 登录连接、M2 费用报销审核与安全写回、M3 发票台账稽核。HTML 是明确标识为虚拟数据的演示原型；EXE 登录后从操作者填写的 ERP API 实时拉取，不内置 API Key、待审队列、发票台账或历史人工操作缓存。

## 验收结论

- 自动测试：37/37 通过；
- 隔离 Docker 公开样本：30/30 判定一致、30/30 违规代码一致；
- 隔离 Docker 全量 M2：300/300、0 失败；
- 隔离 Docker M3：23,461 条台账、40 组重复票、110 条正式问题；
- 写回安全：仅对一张显式指定的隔离测试单写入审核意见，读回成功；再次请求跳过已有相同意见。

## 上传前人工补项

代码仓库地址为 `https://github.com/c2661210488-glitch/qiheng-finance-workbench`，正式 `submission.json` 已填写并通过官方校验器。请将准备好的源码文件夹上传到该仓库；视频由项目成员录制并按课程要求补入。

## 使用入口

1. 先按《部署说明》启动导师提供的 Docker/ERP；
2. 启动 EXE，填写 API 地址和当前操作者创建的 API Key；
3. 在 M2 完成人工确认后才进入“批量写回 ERP”；
4. 在 M3 点击“读取全量发票，开始稽核”，该模块只形成台账任务，不回写 ERP。
